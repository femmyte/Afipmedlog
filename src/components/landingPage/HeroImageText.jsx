import React from "react";

const HeroImageText = (props) => {
  return (
    <div>
      <div>
        <p>{props.text}</p>
        <p>{props.text}</p>
        <p>{props.text}</p>
        <p>{props.text}</p>
      </div>
    </div>
  );
};

export default HeroImageText;
